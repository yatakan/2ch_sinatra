<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>三浦ちゃんねる</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="/javascript/link.js" type="text/javascript"></script>
  </head>

  <body>
    <div class="thread" id="<%= @threads[0]["id"] %>">
      <h1><%= @threads[0]["title"] %></h1>
      <ul>
        <li>1</li>
        <li><a href="mailto:<%= @threads[0]["mail"] %>"><%= @threads[0]["name"] %></a></li>
        <li><%= datetime(@threads[0]["created_at"]) %></li>
        <li>ID: Nanashi01</li>
        <li class="text"><%= @threads[0]["text"] %></li>
      </ul>
        <% @responses.each.with_index(2) do |response, i| %>
          <ul>
            <li><%= i %></li>
            <li><a href="mailto:<%= response["mail"] %>"><%= response["name"] %></a></li>
            <li><%= datetime(response["created_at"]) %></li>
            <li>ID: Nanashi01</li>
            <li class="text"><%= response["text"] %></a></li>
          </ul>
        <% end %>
      <p><a href="/responses/new/<%= @threads[0]["id"] %>">このスレッドに書き込む</a></p>
      <p><a href="/board">スレッド一覧へ戻る</a></p>
    </div>
    <footer>
    </footer>
  </body>
</html>
